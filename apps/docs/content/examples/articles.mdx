---
title: Fetch Articles
description: Complete guide to fetching, filtering, and managing articles with the Simplist SDK
category: Examples
order: 20
lastModified: 2026-01-05
---

# Articles API

The Articles API provides methods to fetch, filter, search, and list articles from your Simplist project.

## Quick Reference

<ApiMethodTable
  methods={[
    { method: "list", description: "List articles with pagination and filters", returns: "ArticleListItem[]" },
    { method: "get", description: "Get a single article by slug", returns: "Article" },
    { method: "search", description: "Search articles by query", returns: "ArticleListItem[]" },
    { method: "published", description: "Get only published articles", returns: "ArticleListItem[]" },
    { method: "latest", description: "Get latest articles", returns: "ArticleListItem[]" },
    { method: "popular", description: "Get popular articles", returns: "ArticleListItem[]" },
    { method: "rss", description: "Generate RSS feed XML", returns: "string" }
  ]}
/>

## Get a Single Article

Fetch a complete article by its unique slug, including full content.

<CodeBlock language="typescript" filename="get-article.ts">
{`
  import { SimplistClient } from "@simplist.blog/sdk"

  // Uses SIMPLIST_API_KEY from environment variables
  const client = new SimplistClient()

  const response = await client.articles.get('my-article-slug')

  if (response.data) {
    const article = response.data
    console.log(article.title)
    console.log(article.content)
    console.log(article.author.name)
    console.log(\`\${article.readTimeMinutes} min read\`)
}
`}
</CodeBlock>

### Article Object

<TypeTable
  title="Article Type"
  properties={[
    { name: "id", type: "string", description: "Unique article ID", required: true },
    { name: "title", type: "string", description: "Article title", required: true },
    { name: "slug", type: "string", description: "URL-friendly identifier", required: true },
    { name: "content", type: "string", description: "Full article content in Markdown", required: true },
    { name: "excerpt", type: "string | null", description: "Short summary" },
    { name: "coverImage", type: "string | null", description: "Cover image URL" },
    { name: "published", type: "boolean", description: "Publication status", required: true },
    { name: "status", type: "string", description: "Current status", required: true },
    { name: "viewCount", type: "number", description: "Total views", required: true },
    { name: "wordCount", type: "number", description: "Word count", required: true },
    { name: "characterCount", type: "number", description: "Character count", required: true },
    { name: "lineCount", type: "number", description: "Line count", required: true },
    { name: "readTimeMinutes", type: "number", description: "Estimated reading time", required: true },
    { name: "author", type: "Author", description: "Author information", required: true },
    { name: "lastUpdatedBy", type: "Author | null", description: "Last editor" },
    { name: "createdAt", type: "string", description: "Creation timestamp (ISO 8601)", required: true },
    { name: "updatedAt", type: "string", description: "Last update timestamp", required: true },
    { name: "publishedAt", type: "string | null", description: "Publication timestamp" },
    { name: "variants", type: "Record<LanguageCode, ArticleVariant>", description: "Multilingual variants" }
  ]}
/>

## List Articles

Fetch multiple articles with pagination, sorting, and filtering.

<CodeBlock language="typescript" filename="list-articles.ts">
{`
  const response = await client.articles.list({
    page: 1,
    limit: 20,
    sort: 'createdAt',
    order: 'desc',
    published: true
  })

  if (response.data) {
    const articles = response.data
    const { page, limit, total, totalPages } = response.meta

    console.log(\`Page \${page} of \${totalPages}\`)
    console.log(\`Showing \${articles.length} of \${total} articles\`)
  }
`}
</CodeBlock>

### Query Parameters

<TypeTable
  title="ArticleListParams"
  properties={[
    { name: "page", type: "number", description: "Page number for pagination", defaultValue: "1" },
    { name: "limit", type: "number", description: "Items per page", defaultValue: "10" },
    { name: "sort", type: "'createdAt' | 'updatedAt' | 'title'", description: "Field to sort by", defaultValue: "'createdAt'" },
    { name: "order", type: "'asc' | 'desc'", description: "Sort order", defaultValue: "'desc'" },
    { name: "published", type: "boolean", description: "Filter by publication status" },
    { name: "status", type: "'draft' | 'published'", description: "Filter by specific status" },
    { name: "search", type: "string", description: "Search query" }
  ]}
/>

### Pagination Metadata

List methods return a `meta` object with pagination information:

<CodeBlock language="typescript" filename="pagination.ts">
{`
  {
    page: 1,           // Current page
    limit: 20,         // Items per page
    total: 157,        // Total items
    totalPages: 8      // Total pages
  }
`}
</CodeBlock>

## Latest Articles

Get the most recent published articles.

<CodeBlock language="typescript" filename="latest.ts">
{`
  // Get 10 latest articles
  const response = await client.articles.latest(10)

  // Custom limit
  const response = await client.articles.latest(25)
`}
</CodeBlock>

This method automatically:
- Filters to **published articles only**
- Sorts by **creation date** (newest first)
- Applies the specified limit

## Published Articles

Fetch only published articles with custom filters.

<CodeBlock language="typescript" filename="published.ts">
{`
  const response = await client.articles.published({
    page: 1,
    limit: 20,
    sort: 'title',
    order: 'asc'
  })

  // All parameters are optional
  const response = await client.articles.published()
`}
</CodeBlock>

This method is equivalent to `list()` with `published: true`.

## Popular Articles

Get the most viewed articles.

<CodeBlock language="typescript" filename="popular.ts">
{`
  // Get 10 most popular articles
  const response = await client.articles.popular(10)

  // Custom limit
  const response = await client.articles.popular(20)
`}
</CodeBlock>

**Note:** This currently returns published articles. Sorting by view count may be added in a future API update.

## Search Articles

Search articles by title, content, or excerpt.

<CodeBlock language="typescript" filename="search.ts">
{`
  // Simple search
  const results = await client.articles.search('React hooks')

  // Search with filters
  const results = await client.articles.search('React hooks', {
    page: 1,
    limit: 10,
    sort: 'createdAt',
    order: 'desc'
  })
`}
</CodeBlock>

The search query matches against:
- Article **title**
- Article **excerpt**
- Article **content**

## Generate RSS Feed

Create an RSS 2.0 XML feed from your articles.

<CodeBlock language="typescript" filename="rss.ts">
{`
  const xml = await client.articles.rss({
    hostname: 'https://myblog.com',
    title: 'My Blog',
    description: 'Latest articles from my blog',
    limit: 50
  })

  // In Next.js API route
  export async function GET() {
    const xml = await client.articles.rss({
      hostname: process.env.NEXT_PUBLIC_URL
    })

    return new Response(xml, {
      headers: {
        'Content-Type': 'application/xml',
        'Cache-Control': 'public, max-age=3600'
      }
    })
  }
`}
</CodeBlock>

### RSS Options

<TypeTable
  title="RSS Feed Options"
  properties={[
    { name: "hostname", type: "string", description: "Base URL for article links", required: true },
    { name: "title", type: "string", description: "Feed title", defaultValue: '"Blog Feed"' },
    { name: "description", type: "string", description: "Feed description", defaultValue: '"Latest articles"' },
    { name: "limit", type: "number", description: "Max articles", defaultValue: "50" }
  ]}
/>

## Error Handling

All methods return an `ApiResponse<T>` with error handling built in.

<CodeBlock language="typescript" filename="error-handling.ts">
{`
  try {
    const response = await client.articles.get('article-slug')

    if (response.data) {
      console.log(response.data.title)
    }
  } catch (error) {
    if (error instanceof SimplistApiError) {
      console.error(\`API Error [\${error.statusCode}]: \${error.message}\`)
    } else {
      console.error('Unexpected error:', error)
    }
  }
`}
</CodeBlock>

### Common Errors

<ErrorTable
  title="HTTP Error Status Codes"
  errors={[
    { status: "404", error: "Article not found", solution: "Check the slug is correct" },
    { status: "401", error: "Unauthorized", solution: "Verify your API key" },
    { status: "403", error: "Forbidden", solution: "Check API key permissions" },
    { status: "429", error: "Rate limit exceeded", solution: "Wait before retrying" },
    { status: "500", error: "Server error", solution: "Retry with exponential backoff" }
  ]}
/>

## Complete Example

<CodeBlock language="typescript" filename="complete-example.ts">
{`
  import { SimplistClient } from "@simplist.blog/sdk"

  const client = new SimplistClient({
    apiKey: process.env.SIMPLIST_API_KEY
  })

  // Fetch latest articles for homepage
  async function getHomepageArticles() {
    const response = await client.articles.latest(6)
    return response.data || []
  }

  // Fetch article for blog post page
  async function getArticle(slug: string) {
    const response = await client.articles.get(slug)

    if (!response.data) {
      return null
    }

    return response.data
  }

  // Paginated article list
  async function getArticlePage(page: number = 1) {
    const response = await client.articles.published({
      page,
      limit: 12,
      sort: 'publishedAt',
      order: 'desc'
    })

    return {
      articles: response.data || [],
      pagination: response.meta
    }
  }

  // Search functionality
  async function searchArticles(query: string) {
    if (!query.trim()) {
      return []
    }

    const response = await client.articles.search(query, {
      limit: 20
    })

    return response.data || []
  }
`}
</CodeBlock>

## Next Steps

<BlockLink
  items={[
    {
      title: "Analytics",
      href: "/analytics",
      description: "Track page views and engagement"
    },
    {
      title: "Multilingual Support",
      href: "/multilingual",
      description: "Handle translated content"
    },
    {
      title: "SEO Utilities",
      href: "/seo",
      description: "Generate sitemaps and meta tags"
    }
  ]}
/>

## Frequently Asked Questions

<Faq
  title="Quick Help"
  items={[
    {
      question: "Should I use ISR or SSR for article pages?",
      answer: "Use ISR (Incremental Static Regeneration) with revalidate for better performance. Set revalidate: 60 to rebuild pages every minute, or use on-demand revalidation when content changes."
    },
    {
      question: "How do I handle article not found (404)?",
      answer: "Wrap the SDK call in try-catch and use Next.js notFound() function for 404 errors. This shows your custom 404 page instead of an error page."
    },
    {
      question: "Can I fetch articles at build time?",
      answer: "Yes! Use the SDK in generateStaticParams() to pre-render all articles at build time. This is perfect for static blogs with infrequent updates."
    }
  ]}
/>